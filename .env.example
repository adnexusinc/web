# Frontend-safe public keys only
# NEVER commit real keys to git

# Stripe (public key only - safe for frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Supabase (public keys - safe for frontend)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Backend API URL (if using separate backend)
VITE_API_URL=https://api.ad.nexus

# DO NOT PUT THESE IN FRONTEND:
# - STRIPE_SECRET_KEY (backend only!)
# - SUPABASE_SERVICE_KEY (backend only!)
# - Any other secret keys